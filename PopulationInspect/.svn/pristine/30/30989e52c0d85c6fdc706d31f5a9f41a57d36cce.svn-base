CREATE VIEW  VIEW_DAYS_SINCE_LAST_INSPECT AS
SELECT 
	RM.ROOM_ID AS ROOM_ID,
    BD.BUIL_ID AS BUIL_ID,
    BD.COMM_ID AS COMM_ID,
    (to_days(now()) - to_days(RM.TIME_LASTINSPECT)) AS DAYS_LAST_INSPECT,
    BD.POLI_ID AS POLI_ID
FROM room AS RM ,building AS BD
WHERE
	RM.BUIL_ID = BD.BUIL_ID